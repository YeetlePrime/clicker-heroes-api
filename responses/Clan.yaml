type: object
required:
  - success
  - reason
properties:
  success:
    type: boolean
    enum: [true]
  result:
    type: object
    required:
      - guild
      - guildMembers
    properties:
      guild:
        $ref: '../entities/ClanInfo.yaml'
      guildMembers:
        $ref: '../entities/ClanMembers.yaml'
    description: "Info on the clan"

