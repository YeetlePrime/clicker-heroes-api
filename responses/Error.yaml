type: object
required:
  - success
  - reason
properties:
  success:
    type: boolean
    enum: [false]
  reason:
    type: string
    description: "Reason why the operation failed"
example:
  success: false
  reason: "Password mismatch"
